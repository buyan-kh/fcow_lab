export function InsightCard({ refreshed }: { refreshed: boolean }) {
  return <section className="panel insight-panel" aria-labelledby="insight-title"><div className="insight-orbit" aria-hidden="true"><span>✦</span><i /><i /></div><div className="insight-copy"><span className="muted-kicker">Model insight {refreshed ? '· just updated' : '· 2 min ago'}</span><h2 id="insight-title">{refreshed ? 'The translation gap is now the gating variable.' : 'The program is not blocked by target relevance.'}</h2><p>{refreshed ? 'A paired donor study has the highest chance of changing the next capital decision.' : 'It is blocked by confidence that the mechanism will hold outside one reference model.'}</p></div><div className="insight-footer"><span className="tool-chip">Reasoning trace</span><button className="text-button">Open trace <span aria-hidden="true">↗</span></button></div></section>;
}
